## Verification (generated — do not retype)

Produced by `verify-migration.py` from the target tree AND `migration-log/run.log`, the two oracles. **138 IDENTISCH · 23 DIVERGIERT · 51 NICHT PRÜFBAR.**

Verdicts: **IDENTISCH** = matches the source · **DIVERGIERT** = differs, named below · **NICHT PRÜFBAR** = could not be checked, which is **not** a pass and owes a named human an action.

| Layer | Check | What it asks | IDENTISCH | DIVERGIERT | NICHT PRÜFBAR |
|---|---|---|---|---|---|
| conservation | C1 | every source artefact still exists in the migrated module | 1 | 0 | 0 |
| conservation | C2 | every artefact is reachable from the rendered Artifacts page | 2 | 0 | 0 |
| conservation | C3 | every source guide page was migrated or explicitly retired | 43 | 0 | 0 |
| conservation | C4 | the source's narrative text is present somewhere in the target | 8 | 14 | 21 |
| conservation | C5 | menus lead somewhere, and every page is in a menu | 2 | 2 | 0 |
| conservation | C6 | each text passage landed on the page the page map promised | 37 | 0 | 0 |
| conservation | C7 | content the migration wrote is marked as such in the guide | 1 | 0 | 20 |
| fidelity | F1 | module identity is unchanged (id, canonical, version, licence, ...) | 5 | 2 | 2 |
| fidelity | F2 | dependency versions are pinned exactly as the source pinned them | 1 | 0 | 3 |
| fidelity | F3 | the licence is asserted from evidence, never defaulted | 1 | 0 | 1 |
| fidelity | F4 | no mechanical FSH conversion residue is left | 1 | 0 | 0 |
| provenance | P1 | the rendered site reports the template package it was built with | 1 | 0 | 0 |
| provenance | P2 | the vendored template ref matches what the run log recorded | 1 | 0 | 0 |
| provenance | P3 | the IG Publisher version matches the workflow pin | 1 | 0 | 0 |
| provenance | P4 | the source guide was pinned to a published version, not 'current' | 1 | 0 | 0 |
| provenance | P5 | ig.ini points at the IG resource the build actually writes | 1 | 0 | 0 |
| rendering | R1 | tables, tabs and images render with content, not empty | 2 | 0 | 1 |
| rendering | R2 | page header and footer metadata render correctly | 0 | 2 | 0 |
| rendering | R3 | a translated page really differs from the default language | 1 | 0 | 0 |
| rendering | R4 | no links point at template example artefacts that were deleted | 1 | 0 | 0 |
| rendering | R5 | every page has a title unit in the translation catalogue | 1 | 0 | 0 |
| log | L0 | a run log exists at all | 1 | 0 | 0 |
| log | L1 | every partial-success warning was acted on | 1 | 0 | 0 |
| log | L2 | every expected step actually wrote a log line | 22 | 0 | 1 |
| log | L3 | no identity contradiction is still open | 1 | 3 | 0 |
| log | L4 | the log's counts agree with what the tree holds | 1 | 0 | 2 |

### DIVERGIERT — each one a stop or a recorded decision

| id | Check | What it asks | Subject | Evidence | Next action | Auto-fixable |
|---|---|---|---|---|---|---|
| `C4-518fef` | C4 | the source's narrative text is present somewhere in the target | Anwendungsfaelle-Informationsmodell/Index.page.md | 1 of 2 PROSE runs of the source page are in no target page (first: Dieses Modul ist von entscheidender Bedeutung, da es die Gru…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-85a41b` | C4 | the source's narrative text is present somewhere in the target | Beschreibung-von-Szenarien-fuer-die-Anwendung-der-Module.page.md | 2 of 7 PROSE runs of the source page are in no target page (first: Die Ergebnisse von Empfindlichkeitstests können sowohl gemäß…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-196ae3` | C4 | the source's narrative text is present somewhere in the target | CapabilityStatement.page.md | 2 of 6 PROSE runs of the source page are in no target page (first: Um eine dezentrale Datenauswertung mittel des Deutsche Forsc…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-90cec4` | C4 | the source's narrative text is present somewhere in the target | Changelog.page.md | 2 of 89 PROSE runs of the source page are in no target page (first: tbd - Das Logical Model war bisher ein Verzeichnis der FHIR-…); 172 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-6f76e9` | C4 | the source's narrative text is present somewhere in the target | FQL-Beschreibung.page.md | 2 of 2 PROSE runs of the source page are in no target page (first: from StructureDefinition where url = %canonical select Besch…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-c13868` | C4 | the source's narrative text is present somewhere in the target | FQL-Capability-Operations.page.md | 1 of 1 PROSE runs of the source page are in no target page (first: from CapabilityStatement where url = %capability for rest.re…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-74f5d8` | C4 | the source's narrative text is present somewhere in the target | FQL-Capability-REST.page.md | 1 of 1 PROSE runs of the source page are in no target page (first: from CapabilityStatement where url = %capability for rest.re…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-dbb5f9` | C4 | the source's narrative text is present somewhere in the target | FQL-Capability-Search.page.md | 1 of 1 PROSE runs of the source page are in no target page (first: from CapabilityStatement where url = %capability for rest.re…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-b7b687` | C4 | the source's narrative text is present somewhere in the target | Kontext-im-Gesamtprojekt-Bezuege-zu-anderen-Modulen.page.md | 5 of 5 PROSE runs of the source page are in no target page (first: Dieses KDS-Modul ist eng mit dem [KDS-Modul Laborbefund]( "K…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-729ff0` | C4 | the source's narrative text is present somewhere in the target | MIIIGModulMikrobiologie/Index.page.md | 11 of 15 PROSE runs of the source page are in no target page (first: Dieser Leitfaden ist im Rahmen der Medizininformatik-Initati…); 9 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-ba5722` | C4 | the source's narrative text is present somewhere in the target | Probe.page.md | 1 of 8 PROSE runs of the source page are in no target page (first: Für die Abbildung von Proben wird auf das Profil [MII_PR_Bio…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-e7d0b5` | C4 | the source's narrative text is present somewhere in the target | Profilauswahl-und-Abgrenzung.page.md | 2 of 31 PROSE runs of the source page are in no target page (first: Für gramnegative Erreger mit einer MRGN-Klassifikation erfol…); 19 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-fe91db` | C4 | the source's narrative text is present somewhere in the target | Referenzen.page.md | 1 of 3 PROSE runs of the source page are in no target page (first: Es baut auf den Vorarbeiten für den [HiGHmed Use Case Infect…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-d47516` | C4 | the source's narrative text is present somewhere in the target | UML.page.md | 1 of 10 PROSE runs of the source page are in no target page (first: Zur besseren Lesbarkeit findet sich das vollständige Diagram…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C5-87e969` | C5 | menus lead somewhere, and every page is in a menu | input/pagecontent/ImplementationGuide-kerndatensatzmodul-mikrobiologie.md | narrative page in NO menu entry -- rendered, but reachable only by typing its URL | add it to input/includes/menu.xml (and the per-language copies), or retire the page | no |
| `C5-89930f` | C5 | menus lead somewhere, and every page is in a menu | input/pagecontent/ImplementationGuide-kerndatensatzmodul-mikrobiologie.md | target page is neither a template page nor the target of any page-map row -- it appeared during migration | name its provenance in the page map, or remove it; a page nobody can trace to a source is content a reviewer cannot check | no |
| `F1-bacf3c` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | packageId | target de.medizininformatikinitiative.kerndatensatz.mikrobiologie  vs  source project | the SOURCE wins (spec 2.2); restore it or record the divergence as a Gate-A decision -- never normalise silently | no |
| `F1-b5b102` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | title | target MII Implementation Guide Microbiology  vs  source kerndatensatzmodul-mikrobiologie | the SOURCE wins (spec 2.2); restore it or record the divergence as a Gate-A decision -- never normalise silently | no |
| `R2-48ed52` | R2 | page header and footer metadata render correctly | output/de id="ig-status" [{{] | on 1 page(s), e.g. searchform.html: Search {{title}} (Current Build) | rendered header/footer metadata defect -- qa.txt does not report it. Fix the metadata it renders (a jurisdiction code the template cannot resolve is the measured case) | no |
| `R2-d1da9a` | R2 | page header and footer metadata render correctly | output/en id="ig-status" [{{] | on 1 page(s), e.g. searchform.html: Search {{title}} (Current Build) | rendered header/footer metadata defect -- qa.txt does not report it. Fix the metadata it renders (a jurisdiction code the template cannot resolve is the measured case) | no |
| `L3-8d972c` | L3 | no identity contradiction is still open | identity field ? | 1 unresolved contradiction WARN(s), first at 2026-08-25T14:45:07Z: identity-contradiction: releaseLabel=2026.0.0-alpha.6 against version=2027.0.0-alpha.6, both from sushi-config… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |
| `L3-dd7d67` | L3 | no identity contradiction is still open | identity field packageId | 2 unresolved contradiction WARN(s), first at 2026-08-25T14:44:49Z: identity-contradiction: field=packageId now=project (tier J, package.json name) vs packageId=de.medizininforma… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |
| `L3-2981ed` | L3 | no identity contradiction is still open | identity field version | 3 unresolved contradiction WARN(s), first at 2026-08-25T14:44:48Z: identity-contradiction: field=version now=2027.0.0-alpha.6 (tier C, sushi-config.yaml) vs version=2027.0.0-alp… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |

### NICHT PRÜFBAR — not a pass; each needs a human

| id | Check | What it asks | Subject | Why not mechanisable | Who does what |
|---|---|---|---|---|---|
| `C4-5dca0e` | C4 | the source's narrative text is present somewhere in the target | Allgemeine-Bestimmung.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 8 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-ef584a` | C4 | the source's narrative text is present somewhere in the target | Allgemeine-Kultur.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-84dd06` | C4 | the source's narrative text is present somewhere in the target | Antigen-Antikoerper-Quantitativ.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c8c1ba` | C4 | the source's narrative text is present somewhere in the target | Aviditaet.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-971b9a` | C4 | the source's narrative text is present somewhere in the target | Barlett-Score.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-d3b218` | C4 | the source's narrative text is present somewhere in the target | CT-Wert.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-5b1472` | C4 | the source's narrative text is present somewhere in the target | Diagnostic-Report.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 6 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-987b60` | C4 | the source's narrative text is present somewhere in the target | Empfindlichkeit.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 3 of 5 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c27b31` | C4 | the source's narrative text is present somewhere in the target | Keimzahl.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-9ccbe6` | C4 | the source's narrative text is present somewhere in the target | MRGN-Klasse.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 4 of 11 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-4a43b9` | C4 | the source's narrative text is present somewhere in the target | Mikroskopie.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-0aaa28` | C4 | the source's narrative text is present somewhere in the target | Molekulare-Pathogenlast.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-cfbdf9` | C4 | the source's narrative text is present somewhere in the target | Nugent-Score.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-768ed9` | C4 | the source's narrative text is present somewhere in the target | Resistenzkategorie-Status.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 23 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-62691a` | C4 | the source's narrative text is present somewhere in the target | Resistenzmechanismen-Determinanten.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-03eefa` | C4 | the source's narrative text is present somewhere in the target | Spezifische-Bestimmung.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 5 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-f8edf0` | C4 | the source's narrative text is present somewhere in the target | Spezifische-Kultur.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 4 of 9 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-2a8dbd` | C4 | the source's narrative text is present somewhere in the target | Technische-Implementierung/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-ee7ce6` | C4 | the source's narrative text is present somewhere in the target | Titer.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 8 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e50664` | C4 | the source's narrative text is present somewhere in the target | Virulenzfaktor.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-3c1363` | C4 | the source's narrative text is present somewhere in the target | Voraussichtliche-Empfindlichkeit.page.md | the source page embeds an element-tree rendering (4 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C7-304ac6` | C7 | content the migration wrote is marked as such in the guide | Allgemeine-Bestimmung.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-7e4929` | C7 | content the migration wrote is marked as such in the guide | Allgemeine-Kultur.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-8ee41c` | C7 | content the migration wrote is marked as such in the guide | Antigen-Antikoerper-Quantitativ.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-623158` | C7 | content the migration wrote is marked as such in the guide | Aviditaet.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-aa72eb` | C7 | content the migration wrote is marked as such in the guide | Barlett-Score.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-149782` | C7 | content the migration wrote is marked as such in the guide | CT-Wert.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-0e3693` | C7 | content the migration wrote is marked as such in the guide | Diagnostic-Report.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-88fcfb` | C7 | content the migration wrote is marked as such in the guide | Empfindlichkeit.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-b5be01` | C7 | content the migration wrote is marked as such in the guide | Keimzahl.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-290398` | C7 | content the migration wrote is marked as such in the guide | MRGN-Klasse.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-df5e24` | C7 | content the migration wrote is marked as such in the guide | Mikroskopie.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-7ad21b` | C7 | content the migration wrote is marked as such in the guide | Molekulare-Pathogenlast.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-4ee139` | C7 | content the migration wrote is marked as such in the guide | Nugent-Score.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-4c9a58` | C7 | content the migration wrote is marked as such in the guide | Resistenzkategorie-Status.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-8ce896` | C7 | content the migration wrote is marked as such in the guide | Resistenzmechanismen-Determinanten.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-4c36bc` | C7 | content the migration wrote is marked as such in the guide | Spezifische-Bestimmung.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-d7740f` | C7 | content the migration wrote is marked as such in the guide | Spezifische-Kultur.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-209de2` | C7 | content the migration wrote is marked as such in the guide | Titer.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f478aa` | C7 | content the migration wrote is marked as such in the guide | Virulenzfaktor.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f4e5a6` | C7 | content the migration wrote is marked as such in the guide | Voraussichtliche-Empfindlichkeit.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `F1-7bae24` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | license | no source value (neither the source tree nor the claims ledger yields one) | supply it at Gate A -- an identity field nobody can compare is not a field that matches |
| `F1-5451f2` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | publisher | no source value (neither the source tree nor the claims ledger yields one) | supply it at Gate A -- an identity field nobody can compare is not a field that matches |
| `F2-fd0e73` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.crmi | target-only dependency 2.0.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-617227` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.ips | target-only dependency 1.1.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-e78ce4` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.xver-r5.r4 | target-only dependency 0.1.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F3-1a29c4` | F3 | the licence is asserted from evidence, never defaulted | license | target declares CC-BY-4.0 with NO tier evidence behind it (no claim in identity-claims.tsv) | read the source's LICENSE (repo-identity.sh) and claim it, or confirm the value at Gate A. The template's literal CC-BY-4.0 reaches here unflagged otherwise |
| `R1-76f413` | R1 | tables, tabs and images render with content, not empty | source-versus-target rendering | no harvested source HTML (./migration-log/guide-harvest/html) and/or no page map | harvest with --keep-html and write the page map; without a source rendering, 'non-empty where non-empty in the source' has no reference |
| `L2-930ba2` | L2 | every expected step actually wrote a log line | 5.1d guide-harvest | no line in the log; the step is conditional (the narrative is not in the repository) | confirm the condition did not hold -- This is the step whose absence shipped the template's starter pages under a module's name. |
| `L4-106e61` | L4 | the log's counts agree with what the tree holds | conversion count | no `gofsh-convert … actual=` line in the log | shape B only; for shape A there is nothing to convert |
| `L4-f17740` | L4 | the log's counts agree with what the tree holds | page count | no harvested count in the log and/or no harvest manifest | harvest the guide (step 2c) where the narrative is not in the repo |

**Inputs:** target `.` · source `/private/tmp/claude-501/-Users-patrickwerner-IdeaProjects-kerndatensatzmodul-mikrobiologie/a759b1ea-352c-4c43-b7b4-77c7c89c0da5/scratchpad/source-unmigrated` · rendered `/Users/patrickwerner/IdeaProjects/kerndatensatzmodul-mikrobiologie/output` · log `./migration-log/run.log`
